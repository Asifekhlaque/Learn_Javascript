const obj ={
    Asif:52,
    shubham:45,
    virat:30,
    khushi:25
}

for(let i=0;i<Object.keys(obj).length;i++){
    console.log("key",Object.keys(obj)[i],"value",obj[Object.keys(obj)[i]])
}